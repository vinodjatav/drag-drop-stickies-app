export interface Sticky {
  id: number;
  body: string;
  colors: string;
  position: string;
}
export interface Color {
  id: string;
  colorHeader: string;
  colorBody: string;
  colorText: string;
}
export interface MouseMoveDirection {
  x: number;
  y: number;
}
